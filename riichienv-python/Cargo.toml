[package]
name = "riichienv-python"
version = "0.4.8"
edition = "2024"
license = "Apache-2.0"

[lib]
name = "_riichienv"
crate-type = ["cdylib"]

[dependencies]
riichienv-core = { path = "../riichienv-core", features = ["python"] }
riichienv-calc = { path = "../riichienv-calc" }
pyo3 = { version = "0.28" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
